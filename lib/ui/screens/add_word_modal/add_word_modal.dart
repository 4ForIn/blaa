import 'package:flutter/material.dart';

class AddWordModal extends StatelessWidget {
  const AddWordModal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('AddWordModal', style: TextStyle(fontSize: 55)));
  }
}